import { FocusOnLoadDirective } from './focus-on-load.directive';

describe('FocusOnLoadDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusOnLoadDirective();
    expect(directive).toBeTruthy();
  });
});
